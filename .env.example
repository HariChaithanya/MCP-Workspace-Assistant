# LLM provider (required for agent steps later)
OPENAI_API_KEY=your-openai-api-key

# Local workspace folder the assistant can read
WORKSPACE_PATH=./sample-workspace

# Optional: GitHub read-only tools (Step 11)
GITHUB_PERSONAL_ACCESS_TOKEN=
